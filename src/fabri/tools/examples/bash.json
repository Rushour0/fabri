{
  "name": "bash",
  "description": "Run a bash command in the sandbox cwd with timeout + 50KB output truncation. args: {command: bash -c string, timeout?: seconds}. NOT a security sandbox -- only cwd-scoped. Returns {exit_code, stdout, stderr, *_truncated}.",
  "command": ["python3", "bash.py"],
  "input_schema": {
    "type": "object",
    "properties": {"command": {"type": "string"}, "timeout": {"type": "number"}},
    "required": ["command"]
  },
  "output_schema": {"type": "object"},
  "timeout_s": 60
}
