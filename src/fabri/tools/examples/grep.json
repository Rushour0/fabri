{
  "name": "grep",
  "description": "Regex search across sandboxed files. args: {pattern: regex, path?: file or dir (default '.'), glob?: pathspec (default '**/*')}. Returns up to 200 matches as {path, line, text}. Use this before read_file to locate definitions/references without paying token costs for the full file.",
  "command": ["python3", "grep.py"],
  "input_schema": {
    "type": "object",
    "properties": {
      "pattern": {"type": "string"},
      "path": {"type": "string"},
      "glob": {"type": "string"}
    },
    "required": ["pattern"]
  },
  "output_schema": {"type": "object"},
  "timeout_s": 15
}
