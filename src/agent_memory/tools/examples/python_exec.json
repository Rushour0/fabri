{
  "name": "python_exec",
  "description": "Run a Python snippet in a fresh subprocess in the sandbox cwd, with timeout + 50KB output truncation. args: {code: python source, timeout?: seconds}. NOT a security sandbox -- only cwd-scoped.",
  "command": ["python3", "python_exec.py"],
  "input_schema": {
    "type": "object",
    "properties": {"code": {"type": "string"}, "timeout": {"type": "number"}},
    "required": ["code"]
  },
  "output_schema": {"type": "object"},
  "timeout_s": 60
}
