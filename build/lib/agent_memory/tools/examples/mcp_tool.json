{
  "name": "mcp_tool",
  "description": "Generic adapter that calls one tool on any stdio-based MCP server. args: {server: [command, ...args], tool: tool_name, arguments: {...}}. Use this to plug an existing MCP server's tools into this agent without writing a bespoke wrapper per server -- one config-level entry per call, instead of a new Python tool per MCP server.",
  "command": ["python3", "mcp_tool.py"],
  "input_schema": {
    "type": "object",
    "properties": {
      "server": {"type": "array", "items": {"type": "string"}},
      "tool": {"type": "string"},
      "arguments": {"type": "object"}
    },
    "required": ["server", "tool"]
  },
  "output_schema": {"type": "object"},
  "timeout_s": 30
}
