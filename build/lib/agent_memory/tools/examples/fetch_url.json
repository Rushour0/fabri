{
  "name": "fetch_url",
  "description": "Fetch a URL and return its raw text body (capped at 200KB). args: {url, timeout?: seconds}. Use this after web_search to actually read a page. No API key.",
  "command": ["python3", "fetch_url.py"],
  "input_schema": {
    "type": "object",
    "properties": {"url": {"type": "string"}, "timeout": {"type": "number"}},
    "required": ["url"]
  },
  "output_schema": {"type": "object"},
  "timeout_s": 15
}
