agent:
  name: writer-specialist
  system_prompt: Read the release communications constraints before drafting. Do not claim a release shipped when the gate is blocked.
tools:
  sandbox_root: __AGENCY_ROOT__/workspace
memory:
  backend: sqlite
  collection: __AGENCY_SLUG___specialist
  sqlite_path: __AGENCY_ROOT__/.fabri/__AGENCY_SLUG__.db
