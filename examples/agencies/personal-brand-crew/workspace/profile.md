# Personal-brand profile — SAMPLE (replace with your own verified facts)

> Source of truth for the personal-brand crew. Every claim here must be verified — the crew
> is instructed NOT to invent facts beyond this file. It MAY enrich from the Public links via
> fetch_url. This is a fictional example; overwrite every section with your own real details.

## Identity
- Sam Rivera. Based in Berlin (CET). ~5 years professional.
- Self-taught; previously data engineering, now developer tooling.
- Handle to brand: x.com/example (TARGET account, not a source — do not scrape it).

## What I build
Developer tooling and data pipelines, mostly Python and TypeScript. I like turning flaky manual
steps into checked, repeatable automation.

Roles:
- Staff Engineer @ ExampleCorp (current) — internal developer platform.
- Creator of `tinyflow`, an open-source local-first workflow runner.

## Proof (verified — safe to state)
- **tinyflow**: open-source workflow runner; deterministic re-runs from a content-addressed cache;
  10k+ downloads; a test suite that runs every example in CI.
- **ExampleCorp**: built the CI preview-environment system (per-PR isolated envs, auto-teardown);
  cut average PR review setup from ~20 min to under 2.

## The through-line (brand thesis)
Make the slow, error-prone parts of shipping software boring and automatic — but keep a human in
the loop wherever a mistake is expensive.

## Voice
Plain, specific, engineer-to-engineer. Concrete over abstract. Honest about failure. No hype.

## DO NOT claim (hard guardrails for the crew)
- Never invent metrics, follower counts, employers, or client names not listed above.
- Do not overstate scope (say what you owned, not what the team shipped).
- Do not present internal/unreleased systems by name.

## Public links (crew MAY fetch_url these to enrich; never x.com/twitter.com)
- https://github.com/example
- https://github.com/example/tinyflow
