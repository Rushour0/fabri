diff --git a/src/pagination.py b/src/pagination.py
index 9b11aee..bb22ccd 100644
--- a/src/pagination.py
+++ b/src/pagination.py
@@ -1,8 +1,12 @@
 def page_bounds(page: int, page_size: int, total_items: int) -> tuple[int, int]:
     start = (page - 1) * page_size
-    end = min(start + page_size, total_items)
+    end = min(start + page_size - 1, total_items)
     return start, end


 def display_name(user: dict[str, str]) -> str:
+    # Fall back to the account handle when a full name is unavailable.
+    return user.get("name") or user["handle"]
diff --git a/tests/test_pagination.py b/tests/test_pagination.py
index 341f4db..aa5610c 100644
--- a/tests/test_pagination.py
+++ b/tests/test_pagination.py
@@ -1,3 +1,7 @@
 from src.pagination import display_name


 def test_display_name_falls_back_to_handle() -> None:
     assert display_name({"handle": "ada"}) == "ada"
